@model Budget
@{
    Layout = "_Layout";
}

<div class="row">
    <div class="col-md-6">
        <form asp-action="Create">
            @Html.Partial("_BudgetCreateEditPartial", Model)
        </form>
    </div>
</div>
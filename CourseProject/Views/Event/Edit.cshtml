@model Event
@{
    Layout = "_Layout";
}

<div class="row">
    <div class="col-md-6">
        <form asp-action="Edit">
            @Html.Partial("_CreateEditPartial", Model)
        </form>
    </div>
</div>
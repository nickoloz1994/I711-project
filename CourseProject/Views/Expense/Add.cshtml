@model Expense
@{
    Layout = "_Layout";
}

<div class="row">
    <div class="col-md-6">
        <form asp-controller="Expense" asp-action="Add">
            @Html.Partial("_ExpenseCreateEditPartial", Model)
        </form>
    </div>
</div>